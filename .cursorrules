# 1. 核心技术栈强制规定
- Python: >= 3.10
- 依赖管理: 默认假设使用 `uv`，不要提示使用 `pip install`
- 核心库: Pydantic V2 (绝不允许使用 V1 语法如 `@validator`，必须使用 `@field_validator`), httpx, pytest

# 2. 行为准则
- 绝不要猜测依赖：如果 pyproject.toml 里没有某个包，不允许在代码中 import 它。
- 测试优先：在修改业务代码前，必须先确保你的修改不会破坏 `tests/` 目录下的用例。
- 风格对齐：任何新建的服务类或数据处理脚本，必须 1:1 模仿 `examples/` 目录下的错误处理、类型提示（Type Hinting）和日志记录风格。

# 3. 负向约束 (Context Bounds)
- 不允许读取 `logs/` 或 `.venv/` 下的文件作为上下文。
- 当处理数据解析时，不要自己“发明”正则表达式，优先使用 `src/schemas.py` 里的结构化验证。
